@model IEnumerable<Model.PostComments>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdminManagement.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PostCommentContent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PostCommentTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Posts.PostTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Users.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PostCommentContent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PostCommentTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Posts.PostTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Users.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PostCommentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PostCommentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PostCommentID })
        </td>
    </tr>
}

</table>
